namespace {{Namespace}}
{
    {{ TypeAccessModifier }} {% if IsAbstract %}abstract {% endif %}partial class {{ClassName}} {{InheritanceCode}}
    {
    {% for property in Properties -%}
        public {{ property.Type }} {{ property.PropertyName }} { get; {% if property.HasSetter %}set; {% endif %}}{% if property.HasDefaultValue %} = {{ property.DefaultValue }};{% endif -%}
        
    {% endfor -%}
    }
}